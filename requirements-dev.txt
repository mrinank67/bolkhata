# Development-only dependencies. Kept out of requirements.txt so the Vercel
# serverless bundle stays lean -- @vercel/python installs requirements.txt only.
#
# Install locally with:
#   pip install -r requirements.txt -r requirements-dev.txt

pytest==9.1.1
pytest-cov==7.1.0
httpx==0.28.1          # required by fastapi.testclient.TestClient
ruff==0.16.3
pre-commit==4.6.2
